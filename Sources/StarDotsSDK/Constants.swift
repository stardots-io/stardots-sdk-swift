import Foundation

/// StarDots SDK Constants
public struct Constants {
    /// SDK Version
    public static let sdkVersion = "1.0.0"
    
    /// Endpoint Request the endpoint of the stardots server.
    public static let endpoint = "https://api.stardots.io"
    
    /// Default request timeout, unit: seconds
    public static let defaultRequestTimeout: TimeInterval = 30.0
} 